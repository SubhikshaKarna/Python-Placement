def any_base_to_decimal(number,base):
    dec=int(number,base)
    print(dec)

#Driver's code

num=input("Enter the Number to Convert into Decimal:")
base=int(input("Enter the Base:"))
any_base_to_decimal(num,base)